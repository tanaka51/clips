%h1 All Clips

%dl
  - @clips.each do |clip|
    %dt
      = link_to "Clip: #{clip.id}", clip_path(clip)
    %dd
      - code = clip.code.split("\n").take(5).join("\n")
      != CodeRay.scan(code, :ruby).div
